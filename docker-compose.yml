services:
  expense-tracker:
    build: .
    ports:
      - "8051:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DEBUG=false
      - DATABASE_PATH=/app/data/db.sqlite3
      - QINIU_ACCESS_KEY=${QINIU_ACCESS_KEY}
      - QINIU_SECRET_KEY=${QINIU_SECRET_KEY}
      - QINIU_BUCKET=${QINIU_BUCKET}
      - QINIU_ENDPOINT=${QINIU_ENDPOINT:-s3-cn-east-1.qiniucs.com}
    networks:
      - shared_net

networks:
  shared_net:
    external: true
