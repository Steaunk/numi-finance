services:
  expense-tracker:
    build: .
    ports:
      - "8051:8000"
    volumes:
      - ./db.sqlite3:/app/data/db.sqlite3
    environment:
      - DEBUG=false
      - DATABASE_PATH=/app/data/db.sqlite3
    networks:
      - shared_net

networks:
  shared_net:
    external: true
